<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <!-- Style for the dark progress dialog -->
    <style name="DarkProgressDialog" parent="Theme.AppCompat.Light.Dialog.Alert" xmlns:tools="http://schemas.android.com/tools">
        <item name="android:textColor">@color/textPrimaryColor</item>
        <item name="android:background">@color/backgroundColor</item>
        <item name="android:textColorPrimary">@color/textPrimaryColor</item>
        <item name="colorAccent">@color/textInfo</item>
        <item name="android:colorAccent" tools:targetApi="m">@color/textInfo</item>
    </style>

</resources>
